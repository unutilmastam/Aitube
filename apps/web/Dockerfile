# PLACEHOLDER — Phase 2'da React + Vite dashboard shu yerga to'liq qo'shiladi.
# Hozircha docker-compose buzilmasligi uchun minimal static sahifa beriladi.
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
